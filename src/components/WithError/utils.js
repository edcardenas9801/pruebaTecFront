const useStyles = theme => ({
    container: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center'
    },
    logo: {
        width: '80px',
        marginBottom: theme.spacing.unit
    }
});
export default useStyles;